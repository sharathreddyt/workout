{
  "name": "workout-plan",
  "version": "1.0.0",
  "description": "Local development server for workout HTML file",
  "scripts": {
    "dev": "npx http-server . -p 3000 -o",
    "start": "npx http-server . -p 3000",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
